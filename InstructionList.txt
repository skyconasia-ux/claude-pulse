You are working on a new project located at:

C:\Users\quick\LiveVisualUsage

If the structure is not fully created yet, you MUST create it EXACTLY as defined below before doing anything else.

---

STEP 0 — PROJECT INITIALIZATION (MANDATORY)

Create the following structure EXACTLY:

LiveVisualUsage/
│
├── src/
│   ├── monitor/
│   ├── wrapper/
│   ├── server/
│   └── frontend/
│
├── logs/
├── data/
│
├── docs/
│   ├── architecture.md
│   ├── checkpoints.md
│   ├── claude.md
│
├── system/
│   ├── prompts/
│   ├── tasks/
│   ├── pending/
│   └── helpers/
│
├── tools/
│
├── skills/
│   ├── superpowers/
│   └── brainstorm/
│
├── config.json
├── README.md

---

STEP 0.1 — INITIAL FILE CONTENTS

Populate these files:

/docs/architecture.md
# Live Visual Usage Monitor - Architecture
Status: Initializing

/docs/checkpoints.md
# Checkpoints (APPEND ONLY — DO NOT OVERWRITE)

(No checkpoints yet)

/docs/claude.md
# Claude Working Memory

Project: LiveVisualUsage

Rules:
- Do not re-derive architecture once defined
- Always update checkpoints after major progress
- Keep tasks and pending tasks in sync
- Prefer native telemetry over terminal scraping whenever possible

/config.json
{
  "token_threshold": 100000,
  "turn_threshold": 20,
  "refresh_active_ms": 1000,
  "refresh_idle_ms": 5000
}

/README.md
Live Visual Usage Monitor for Claude CLI

---

STEP 0.2 — CREATE INITIAL CHECKPOINT (MANDATORY)

Immediately create a concise checkpoint and save it to:
- /docs/checkpoints.md (append only)
- /docs/claude.md

Format:

Create a concise checkpoint of the current LiveVisualUsage project:
- objective
- completed work
- current progress
- next step

Keep it short and structured.

---

ONLY proceed once:
- structure is created
- files are populated
- checkpoint is written

---

OBJECTIVE:

Build a real-time telemetry and visualization system for Claude Code CLI usage.

---

CRITICAL ENGINEERING REQUIREMENT (SCALABILITY):

System MUST be:
- modular
- loosely coupled
- configuration-driven
- easily extendable

Future upgrades must support:
- new metrics
- other CLI tools
- different UI layers
- different transport mechanisms

---

REAL-TIME REQUIREMENTS:

- MUST be continuously running in the background once started
- MUST update automatically without manual re-execution
- MUST stream updates live while Claude is active, thinking, troubleshooting, building, or idle
- MUST use WebSocket or equivalent push-based transport for live updates, not polling
- ~1 second updates during activity
- adaptive refresh when idle to reduce CPU usage
- detect state transitions (active ↔ idle)
- include reconnection and startup recovery behavior

---

TELEMETRY SOURCE REQUIREMENT:

Use Claude Code’s native telemetry and observability exports as the primary data source whenever available.
Prefer OpenTelemetry-based metrics, logs, and traces over parsing terminal output or scraping the screen.

---

FEATURES:

Track:
- tokens (input/output/total)
- turns
- estimated cost
- session duration
- activity state

Checkpoint trigger:
- turns >= 20
- token threshold exceeded
- spike detection
- long-running task

---

ARCHITECTURE (MANDATORY COMPONENTS):

1. CLI integration layer for Claude Code
2. Telemetry collector
3. Transport layer (WebSocket or equivalent)
4. Frontend dashboard

---

FRONTEND REQUIREMENT:

The system must support two frontends:
- Browser dashboard
- Terminal dashboard

At startup, present a choice:
- browser
- terminal
- both

The chosen frontend(s) must connect to the same live backend and display the same telemetry stream.

Browser mode should prioritize richer visuals and charts.
Terminal mode should prioritize fast, compact, always-visible status output.

---

INITIAL TASK AFTER SETUP:

1. Define architecture → /docs/architecture.md
2. Create checkpoint → /docs/checkpoints.md (append)
3. Update working memory → /docs/claude.md

---

AUTO CHECKPOINT SYSTEM (MANDATORY):

Triggers:
- turns >= 10 (soft)
- turns >= 20 (mandatory)
- 70% tokens (warning)
- 90% tokens (mandatory)
- after major module completion
- long session

Rules:
- append only (never overwrite)
- update claude.md
- log to /logs/checkpoint.log
- cooldown: 3 turns (except critical)

---

VISUAL INTEGRATION:

Emit:
- "checkpoint_event"
Dashboard must display:
- RECOMMEND CHECKPOINT
- CHECKPOINT CREATED

---

ANTI-WASTE GUARD (STRICT):

- DO NOT overengineer
- DO NOT repeat planning
- DO NOT loop
- DO NOT redesign finished parts

If unclear → assume and proceed
If stuck → simplest working solution

Every meaningful milestone MUST:
- produce files/code
- update checkpoints.md
- update claude.md

---

MID-TASK CORRECTION SYSTEM:

If you detect:
- overcomplication
- no file output
- deviation from steps

You MUST:
1. STOP
2. realign to current step
3. continue with simplest approach

---

USER OVERRIDE:

Command:
RESET FOCUS

Behavior:
- stop
- summarize progress
- resume correct step

---

LIVE TOKEN BURN PREDICTOR (MANDATORY)

Implement a prediction system in the telemetry + dashboard:

CALCULATE IN REAL TIME:

- tokens per turn (rolling average)
- tokens per second during active execution
- projected tokens for current task
- estimated remaining tokens before threshold

DISPLAY:

- "Estimated Remaining Capacity"
- "Projected Task Completion Usage"
- "Burn Rate (tokens/sec)"
- "ETA to Threshold"

ALERT LEVELS:

- GREEN → safe
- YELLOW → approaching limit (~70%)
- RED → critical (~90%)

PREDICTIVE BEHAVIOR:

If system detects:
- rapid token increase
- long response generation

Then:

1. emit warning event:
   → "HIGH TOKEN BURN DETECTED"

2. suggest:
   → "Consider checkpoint or task split"

3. reflect this in dashboard visually

---

PERFORMANCE REQUIREMENT:

- prediction must be lightweight
- no heavy computation loops
- reuse existing telemetry data

---

RULES:

- DO NOT explain theory
- DO NOT restructure folders
- WRITE FILES DIRECTLY

Use skills:
- /skills/superpowers/
- /skills/brainstorm/

If those skills are unavailable in the environment, proceed without them.

---

EXECUTION ORDER:

Step 0 → Setup
Step 1 → Architecture
Step 2 → CLI integration
Step 3 → Telemetry collector
Step 4 → WebSocket server
Step 5 → Frontend dashboard

---

GOAL:

Deliver a working MVP with:
- continuous live streaming visuals
- predictive token monitoring
- scalable modular architecture

Act like a senior systems engineer with an IQ of 190, Ship fast. Self-correct. Avoid waste. And also create a repo in GitHub https://github.com/skyconasia-ux?tab=repositories and upload it there for publishing later.
